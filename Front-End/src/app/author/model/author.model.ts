export class Author {
    id?: number;
    name?: string;
    nationality?: string;

    constructor(id?: number, name? : string, nationality? : string) {
      this.id = id;
      this.name = name;
      this.nationality = nationality;
    }
}
